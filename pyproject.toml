[project]
name = "interview-with-roche"
version = "0.1.0"
description = "A simple Django REST API for managing product inventory, built for an interview task."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.2",
    "django-snowflake-backend>=1.0.2",
    "djangorestframework>=3.16.0",
    "snowflake-connector-python>=3.15.0",
    "snowflake-sqlalchemy>=1.7.3",
]
